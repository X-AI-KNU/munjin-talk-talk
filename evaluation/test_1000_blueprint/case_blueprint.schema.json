{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Test 1000 Case Blueprint Row",
  "type": "object",
  "required": [
    "case_id",
    "dataset_role",
    "visit_type",
    "question_id",
    "question_type",
    "dialect_type",
    "dialect_intensity",
    "symptom_group",
    "gold_symptoms",
    "negative_symptoms",
    "status_pattern",
    "expression_policy",
    "difficulty",
    "dialect_source_layer",
    "surface_target",
    "avoid_surface_forms",
    "renderer_notes"
  ],
  "additionalProperties": false,
  "properties": {
    "case_id": { "type": "string", "pattern": "^test_bp_[0-9]{4}$" },
    "dataset_role": { "enum": ["held_out_test"] },
    "visit_type": { "enum": ["초진", "재진"] },
    "question_id": { "enum": ["Q1", "Q3"] },
    "question_type": { "enum": ["chief_complaint", "new_symptoms"] },
    "dialect_type": { "enum": ["standard", "kangwon"] },
    "dialect_intensity": { "enum": ["none", "light", "medium", "strong"] },
    "symptom_group": {
      "enum": [
        "upper_airway_common",
        "cough_sputum_lower_airway",
        "dyspnea_chest_redflag",
        "systemic_course",
        "ent_swallow_voice_eye",
        "cardio_edema_neuro",
        "gi_abdomen_med_context"
      ]
    },
    "gold_symptoms": {
      "type": "array",
      "minItems": 1,
      "maxItems": 3,
      "items": { "type": "string" }
    },
    "negative_symptoms": {
      "type": "array",
      "maxItems": 2,
      "items": { "type": "string" }
    },
    "status_pattern": {
      "enum": [
        "active_current",
        "new_after_followup",
        "persistent_after_followup",
        "worsened_after_followup",
        "denied_negative_context",
        "mixed_resolved_plus_active"
      ]
    },
    "expression_policy": {
      "enum": ["direct_allowed", "lay_preferred", "direct_label_forbidden"]
    },
    "difficulty": { "enum": ["easy", "medium", "hard"] },
    "dialect_source_layer": {
      "enum": [
        "none",
        "rag_pack_anchored",
        "train_validated_medical_colloquial",
        "light_dialect_flavor"
      ]
    },
    "surface_target": {
      "enum": [
        "short_single_clause",
        "two_clause_with_reason_or_timing",
        "mixed_active_and_denied",
        "followup_course_sentence"
      ]
    },
    "avoid_surface_forms": {
      "type": "array",
      "items": { "type": "string" }
    },
    "renderer_notes": { "type": "string" }
  }
}
